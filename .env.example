# GitHub personal access token (scopes: repo, delete_repo)
GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# Absolute path to the directory containing the projects to push
PROJECTS_DIR=/absolute/path/to/projects

# Optional: path to SSH keys (defaults to ~/.ssh)
# SSH_DIR=/home/user/.ssh
