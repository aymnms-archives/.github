# GitHub personal access token (scope requis : repo)
GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# GitHub username to clone projects FROM
SOURCE_GITHUB_USER=aymnms

# Optional: path to SSH keys (defaults to ~/.ssh)
# SSH_DIR=/Users/toi/.ssh
